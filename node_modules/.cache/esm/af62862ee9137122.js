"main";_e91‍.x([["BotPlayer",()=>BotPlayer]]);let Player;_e91‍.w("./Player.js",[["Player",["Player"],function(v){Player=v}]]);let players;_e91‍.w("./Game.js",[["players",["players"],function(v){players=v}]]);


       class BotPlayer extends _e91‍.a("Player",Player) {
  constructor(obj) {
    super(obj);
    this.pass = obj.pass;
    this.timer = setInterval(() => {
      if (!this.move(4)) {
        this.angle = Math.random() * Math.PI * 2;
      }
      if (Math.random() < 0.03) {
        this.shoot();
      }
    }, 1000 / 30);
  }
  remove() {
    super.remove();
    clearInterval(this.timer);
    setTimeout(() => {
      const bot = new BotPlayer({ nickname: this.nickname,pass:"tob" });
      _e91‍.a("players",players)[bot.id] = bot;
    }, 3000);
  }
};_e91‍.u(["BotPlayer"]);
