"main";_9db‍.x([["FIELD_WIDTH",()=>FIELD_WIDTH],["FIELD_HEIGHT",()=>FIELD_HEIGHT],["players",()=>players],["bullets",()=>bullets],["walls",()=>walls],["updatePlayers",()=>updatePlayers],["updateBullets",()=>updateBullets]]);const FIELD_WIDTH = 1000;
       const FIELD_HEIGHT = 1000;
       let players = {};
       let bullets = {};
       let walls = {};
       function updatePlayers() {
  Object.values(players).forEach(player => {
    const movement = player.movement;
    if (movement.forward) {
      player.move(player.speed);
    }
    if (movement.back) {
      player.move(-player.speed);
    }
    if (movement.left) {
      player.angle -= player.rotationSpeed;
    }
    if (movement.right) {
      player.angle += player.rotationSpeed;
    }
  });
}
       function updateBullets() {
  Object.values(bullets).forEach(bullet => {
    if (!bullet.move(bullet.speed)) {
      bullet.remove();
      return;
    }
    Object.values(players).forEach(player => {
      if (bullet.intersect(player)) {
        if (player !== bullet.player) {
          player.damage();
          bullet.remove();
          bullet.player.point += 1;
        }
      }
    });
  });
}

