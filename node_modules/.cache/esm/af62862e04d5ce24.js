"main";_04d‍.x([["BotPlayer",()=>BotPlayer]]);let Player;_04d‍.w("./Player.js",[["Player",["Player"],function(v){Player=v}]]);let players;_04d‍.w("./Game.js",[["players",["players"],function(v){players=v}]]);


       class BotPlayer extends _04d‍.a("Player",Player) {
  constructor(obj) {
    super(obj);
    this.timer = setInterval(() => {
      if (!this.move(4)) {
        this.angle = Math.random() * Math.PI * 2;
      }
      if (Math.random() < 0.03) {
        this.shoot();
      }
    }, 1000 / 30);
  }
  remove() {
    super.remove();
    clearInterval(this.timer);
    setTimeout(() => {
      const bot = new BotPlayer({ nickname: this.nickname });
      _04d‍.a("players",players)[bot.id] = bot;
    }, 3000);
  }
};_04d‍.u(["BotPlayer"]);
