"main";"use strict";;_22f‍.x([["io",()=>io],["savePlayer",()=>savePlayer]]);let express;_22f‍.w("express",[["default",["express"],function(v){express=v}]]);let http;_22f‍.w("http",[["default",["http"],function(v){http=v}]]);let path;_22f‍.w("path",[["default",["path"],function(v){path=v}]]);let socketIO;_22f‍.w("socket.io",[["default",["socketIO"],function(v){socketIO=v}]]);let fs;_22f‍.w("fs",[["default",["fs"],function(v){fs=v}]]);let players,bullets,walls;_22f‍.w("./modules/Game.js",[["players",["players"],function(v){players=v}],["bullets",["bullets"],function(v){bullets=v}],["walls",["walls"],function(v){walls=v}]]);let Player;_22f‍.w("./modules/Player.js",[["Player",["Player"],function(v){Player=v}]]);let BotPlayer;_22f‍.w("./modules/BotPlayer.js",[["BotPlayer",["BotPlayer"],function(v){BotPlayer=v}]]);let runMain;_22f‍.w("module",[["runMain",["runMain"],function(v){runMain=v}]]);
_22f‍.g.console.log(process.version);




const app = _22f‍.a("express",express)();
const server = _22f‍.a("http",http).Server(app);
       const io = _22f‍.a("socketIO",socketIO)(server);;_22f‍.u(["io"]);

//const { promises } = fs;
const promises = _22f‍.a("fs",fs).promises; //require('fs').promises;

let dataPath = _22f‍.a("path",path).join(process.cwd(), "data");
let fileKey = "4nk22tdINY";

function getDbPath(user) {
  return _22f‍.a("path",path).join("./data", user + fileKey + ".json");
}

async function readDataDirectory() {
  return await promises.readdir("./data");
}

function getDbUserData(user) {
  return promises.readFile(getDbPath(user));
}
async function getUserDb(user) {
  
  let x = getDbPath(user);
  _22f‍.g.console.log(`Getting ${user} db: ` + x);
  return JSON.parse(await promises.readFile(getDbPath(user)));
}
async function userMatchesPass(username,pass){
  
}
getUserDb("abc").then(val=>_22f‍.g.console.log(val));

async function writeDbFile(user, obj) {
  try {
    await promises.writeFile(getDbPath(user), JSON.stringify(obj));
  } catch (error) {
    _22f‍.g.console.log(error);
  }
  _22f‍.g.console.info("file created");
}

writeDbFile("me", { a: 5 });







const bot = new (_22f‍.a("BotPlayer",BotPlayer))({ nickname: "bot" });

_22f‍.a("players",players)[bot.id] = bot;

       let savePlayer = function(player) {
  let jso = player.jso();
  let playerData = db
    .get("Players")
    .find({ nickname: player.nickname, pass: player.pass })
    .assign({
      id: player.id,
      Ttl: Date.now(),
      x: player.x,
      y: player.y,
      width: player.width,
      height: player.height,
      angle: player.angle,
      speed: player.speed,
      rotationSpeed: player.rotationSpeed,
      nickname: player.nickname,
      pass: player.pass,
      maxHealth: player.maxHealth,
      point: player.point,
      Level: player.Level,
      Exp: player.Exp,
      Attack: player.Attack,
      Defense: player.Defense
    })
    .write();
};;_22f‍.u(["savePlayer"]);


io.on("connection", function(socket) {
  _22f‍.g.console.log("io.on connection");
  let player = null;
  socket.on("validate-user",config=>{
    _22f‍.g.console.log("validate-user");
    _22f‍.g.console.log(config);
  });
  socket.on("game-start", config => {
    _22f‍.g.console.log("socket on game-start");
    player = new (_22f‍.a("Player",Player))({
      socketId: socket.id,
      nickname: config.nickname,
      pass: config.password
    });

    let playerData = db
      .get("Players")
      .find({ nickname: player.nickname, pass: player.pass })
      .value();
    _22f‍.g.console.log(":::: " + playerData);
    if (playerData !== undefined) {
      playerData.id = player.id;
      player.x = playerData.x;
      player.y = playerData.y;
      player.width = playerData.width;
      player.height = playerData.height;
      player.angle = playerData.angle;
      player.speed = playerData.speed;
      player.rotationSpeed = playerData.rotationSpeed;
      player.nickname = playerData.nickname;
      player.pass = playerData.pass;
      player.maxHealth = playerData.maxHealth;
      player.point = playerData.point;
      player.Level = playerData.Level;
      player.Exp = playerData.Exp;
      player.Attack = playerData.Attack;
      player.Defense = playerData.Defense;
    }
    _22f‍.a("players",players)[player.id] = player;

    //players[player.id].save();
    db.get("Players")
      .push(_22f‍.a("players",players)[player.id].jso())
      .write();
  });
  socket.on("movement", function(movement) {
    if (!player || player.health === 0) {
      return;
    }
    player.movement = movement;
  });
  socket.on("shoot", function() {
    if (!player || player.health === 0) {
      return;
    }
    player.shoot();
  });
  socket.on("disconnect", () => {
    if (!player) {
      return;
    }
    _22f‍.a("players",players)[player.id].save();
    delete _22f‍.a("players",players)[player.id];
    player = null;
  });
});

setInterval(() => {
  Object.values(_22f‍.a("players",players)).forEach(player => {
    const movement = player.movement;
    if (movement.forward) {
      player.move(player.speed);
    }
    if (movement.back) {
      player.move(-player.speed);
    }
    if (movement.left) {
      player.angle -= player.rotationSpeed;
    }
    if (movement.right) {
      player.angle += player.rotationSpeed;
    }
  });
  Object.values(_22f‍.a("bullets",bullets)).forEach(bullet => {
    if (!bullet.move(bullet.speed)) {
      bullet.remove();
      return;
    }
    Object.values(_22f‍.a("players",players)).forEach(player => {
      if (bullet.intersect(player)) {
        if (player !== bullet.player) {
          player.damage();
          bullet.remove();
          bullet.player.point += 1;
        }
      }
    });
  });
  io.sockets.emit("state", _22f‍.a("players",players), _22f‍.a("bullets",bullets), _22f‍.a("walls",walls));
}, 1000 / 20);

app.use("/static", _22f‍.a("express",express).static(__dirname + "/static"));

app.get("/", (request, response) => {
  response.sendFile(_22f‍.a("path",path).join(__dirname, "/static/3d.html"));
});

const port = 3000;
server.listen(port, () => {
  _22f‍.g.console.log(`Starting server on port ${port}`);
});
