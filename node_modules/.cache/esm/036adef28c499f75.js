"main";_8c4‍.x([["Player",()=>Player]]);let GameObject;_8c4‍.w("./GameObject.js",[["GameObject",["GameObject"],function(v){GameObject=v}]]);let FIELD_HEIGHT,FIELD_WIDTH,players,bullets;_8c4‍.w("./Game.js",[["FIELD_HEIGHT",["FIELD_HEIGHT"],function(v){FIELD_HEIGHT=v}],["FIELD_WIDTH",["FIELD_WIDTH"],function(v){FIELD_WIDTH=v}],["players",["players"],function(v){players=v}],["bullets",["bullets"],function(v){bullets=v}]]);let Bullet;_8c4‍.w("./Bullet.js",[["Bullet",["Bullet"],function(v){Bullet=v}]]);let io;_8c4‍.w("../server.js",[["io",["io"],function(v){io=v}]]);




       class Player extends _8c4‍.a("GameObject",GameObject) {
  constructor(obj = {}) {
    super(obj);
    this.socketId = obj.socketId;
    this.nickname = obj.nickname;
    this.pass = obj.pass;
    this.width = 80;
    this.height = 80;
    this.health = this.maxHealth = 10;
    this.bullets = {};
    this.point = 0;
    this.speed = 5;
    this.movement = {};

    do {
      this.x = Math.random() * (_8c4‍.a("FIELD_WIDTH",FIELD_WIDTH) - this.width);
      this.y = Math.random() * (_8c4‍.a("FIELD_HEIGHT",FIELD_HEIGHT) - this.height);
      this.angle = Math.random() * 2 * Math.PI;
    } while (this.intersectWalls());
  }

  shoot() {
    if (Object.keys(this.bullets).length >= 3) {
      return;
    }
    const bullet = new (_8c4‍.a("Bullet",Bullet))({
      x: this.x + this.width / 2,
      y: this.y + this.height / 2,
      angle: this.angle,
      player: this
    });
    bullet.move(this.width / 2);
    this.bullets[bullet.id] = bullet;
    _8c4‍.a("bullets",bullets)[bullet.id] = bullet;
  }
  damage() {
    this.health--;
    if (this.health === 0) {
      this.save();
      this.remove();
    }
  }
  remove() {
    delete _8c4‍.a("players",players)[this.id];
    _8c4‍.a("io",io).to(this.socketId).emit("dead");
  }
  toJSON() {
    return Object.assign(super.toJSON(), {
      health: this.health,
      maxHealth: this.maxHealth,
      socketId: this.socketId,
      point: this.point,
      nickname: this.nickname
    });
  }
  jso(){
    return {
      id: this.id,
      Ttl:Date.now(),
      x: this.x,
      y: this.y,
      width: this.width,
      height: this.height,
      angle: this.angle,
      speed: this.speed,
      rotationSpeed: this.rotationSpeed,
      nickname: this.nickname,
      pass: this.pass,
      maxHealth: this.maxHealth,
      point: this.point,
      Level: this.Level,
      Exp: this.Exp,
      Attack: this.Attack,
      Defense: this.Defense}
  }
};_8c4‍.u(["Player"]);
